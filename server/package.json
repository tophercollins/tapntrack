{
  "name": "tapntrack-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted single-user API for Tap N Track (Postgres-backed).",
  "scripts": {
    "start": "node --env-file=.env src/index.js",
    "seed": "node --env-file=.env src/seed.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
